{
    int x;
    set s;
    x=10;
    s={1,2};
    {
        set x; // 合法：遮蔽外层int x
        x={5,6}; // 此时x是集合
        add x 7;
        write x; // 输出{5,6,7}
        write s; // 合法：内层未声明s，向上查找外层s
    }
    write x; // 输出10：内层作用域销毁，x恢复为int
}
